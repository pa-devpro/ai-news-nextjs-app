import NewsArchive from './_components/news-archive';

export const metadata = {
  title: 'News Archive',
  description: 'Archive with all the news user saved',
};

const NewsArchivePage = () => {
  return <NewsArchive />;
};

export default NewsArchivePage;
